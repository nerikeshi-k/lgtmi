<!DOCTYPE html>
<html lang="ja">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <meta http-equiv="X-UA-Compatible" content="ie=edge" />
    <link href="https://fonts.googleapis.com/css?family=Roboto:900" rel="stylesheet" />
    <style>
      *,
      *::before,
      *::after {
        box-sizing: border-box;
      }

      html,
      body {
        margin: 0;
        padding: 0;
        font-family: 'Roboto', sans-serif;
        background-color: #ccc;
      }

      main {
        position: relative;
      }

      .lgtm {
        position: absolute;
        z-index: 100;
        width: 100%;
        height: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
        color: white;
        font-size: 25vw;
        font-weight: bold;
        letter-spacing: 0.1em;
      }

      .lgtm.shadow {
        position: absolute;
        z-index: 90;
        width: 100%;
        height: 100%;
        display: flex;
        justify-content: center;
        align-items: center;
        color: #ccc;
        font-size: 25vw;
        font-weight: bold;
        letter-spacing: 0.1em;
        transform: translate(1%, 1%);
      }

      .image {
        width: 100%;
        height: 100%;
      }
    </style>
  </head>
  <body>
    <main style="width: <%= width %>px; height: <%= height %>px;">
      <div class="lgtm">LGTM</div>
      <div class="lgtm shadow">LGTM</div>
      <img class="image" src="<%= url %>" />
    </main>
  </body>
</html>
